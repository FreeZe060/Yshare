<!DOCTYPE html>
<html lang="fr">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, user-scalable=no">
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
	<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
	<script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
	<link rel="stylesheet" type="text/css" href="../static/css/style.css">
</head>


<body>
	<%- include('./partials/header.ejs') %>

		<section id="home" class="home-section d-flex align-items-center">
			<div class="container">
				<div class="row">
					<div class="d-flex flex-column justify-content-center align-items-center ms-auto">
						<h1 class="greeting">Bienvenue sur Y-Share !</h1>
						<h2 class="intro">Ici en organise des rassemblements avec les <span>étudiants</span> de Ynov
						</h2>
						<div class="buttons mt-4">
							<a href="#contact" class="btn btn-warning">Obtenir un billet</a>
							<a href="#projects" class="btn btn-outline-light">Voir Plus</a>
						</div>
					</div>
				</div>
			</div>

			<div class="cont-searchbar d-flex justify-content-between align-items-center py-3">
				<div class="search-event">
					<label for="event-search" class="form-label">Search Event</label>
					<input type="text" id="event-search" class="form-control event-input"
						placeholder="Enter event name..." size="15">
				</div>

				<div class="search-country">
					<label for="country-select" class="form-label">Country</label>
					<select id="country-select" class="form-select country-input">
						<option value="">Select a country</option>
						<option value="france">France</option>
						<option value="usa">USA</option>
						<option value="uk">UK</option>
					</select>
				</div>

				<div class="search-time">
					<label for="date-select" class="form-label">Time</label>
					<input type="date" id="date-select" class="form-control time-input">
				</div>
			</div>
		</section>

		<section class="container_event">
			<div class="container_filter">
				<h2 class="text-center">Upcoming Events</h2>
				<div class="filters">
					<select id="filter-days">
						<option value="">Filter by Days</option>
						<option value="1">1 Day</option>
						<option value="7">7 Days</option>
						<option value="30">30 Days</option>
					</select>

					<select id="filter-type">
						<option value="">Filter by Type</option>
						<option value="conference">Conference</option>
						<option value="workshop">Workshop</option>
						<option value="webinar">Webinar</option>
					</select>

					<select id="filter-category">
						<option value="">Filter by Category</option>
						<option value="technology">Technology</option>
						<option value="health">Health</option>
						<option value="education">Education</option>
					</select>
				</div>
			</div>

			<div class="container_events">
				<% events.forEach(function(event, index) { %>
					<div class="card" onclick="openProjectPopup(<%= JSON.stringify(event) %>)">
						<img src="https://via.placeholder.com/150" class="card-img-top" alt="<%= event.title %>">
						<div class="card-body">
							<div class="body_date">
								<div style="display: flex; flex-direction:column; justify-content: center;">
									<h3><%= new Date(event.date).toLocaleString('default', { month: 'short' }).toUpperCase() %></h3>
									<h2><%= new Date(event.date).getDate() %></h2>
								</div>
							</div>
							<div class="body_text">
								<h5 class="card-title"><%= event.title %></h5>
								<p class="card-text"><%= event.desc %></p>
							</div>
						</div>
					</div>
				<% }); %>
			</div>
		</section>
		
		<div id="projectPopup" class="popup" data-aos="slide-up" onclick="closeProjectPopup(event)">
			<div id="popup-background" class="popup-background">
				<div id="popup-content" class="popup-content" onclick="event.stopPropagation()">
					<div class="popup-header">
				  		<h2 class="popup-title"></h2>
					</div>
					<div class="popup-date-location">
				  		<span class="popup-date"></span>
				  		<span class="popup-location"></span>
					</div>
					<div class="popup-description">
				  		<p></p>
					</div>
			  	</div>
			</div>
		</div>

		<%- include('./partials/footer.ejs') %>

		<script src="/static/js/script.js" defer></script>
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
	</body>

</html>